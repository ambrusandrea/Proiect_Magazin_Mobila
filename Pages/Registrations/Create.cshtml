@page
@model Proiect_Magazin_Mobila.Pages.Registrations.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Registration</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Registration.MemberID" class="control-label"></label>
                <select asp-for="Registration.MemberID" class ="form-control" asp-items="ViewBag.MemberID"></select>
            </div>
            <div class="form-group">
                <label asp-for="Registration.FurnitureID" class="control-label"></label>
                <select asp-for="Registration.FurnitureID" class ="form-control" asp-items="ViewBag.FurnitureID"></select>
            </div>
            <div class="form-group">
                <label asp-for="Registration.ReturnDate" class="control-label"></label>
                <input asp-for="Registration.ReturnDate" class="form-control" />
                <span asp-validation-for="Registration.ReturnDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
